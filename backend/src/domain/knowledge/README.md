# @deprecated — Módulo Knowledge

Este módulo ha sido reemplazado por:

- `src/core/knowledge/` — KnowledgeEngine, KnowledgeRegistry, VersionManager, ApprovalService

**Motivo:** La implementación en `core/` tiene controllers, routes, flujo de aprobación, versionado semver y está registrada en el router principal.

**Plan de eliminación:** Se eliminará en M2.
